os: linux
sudo: required
dist: trusty

notifications:
  email: false

language: C

before_install:
  - sudo apt-get install -y -q mpich libmpich-dev doxygen
  - test -n $CC  && unset CC
  - test -n $CXX && unset CXX
  - mkdir build

script:
  - cd build
  - cmake ..
